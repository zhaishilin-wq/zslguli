spring:
  application:
    name: mall-third-party
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        namespace:
        file-extension: yaml
        extension-configs:
          - data-id: oss.yaml
            group: DEFAULT_GROUP
            refresh: true
